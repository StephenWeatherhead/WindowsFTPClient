<Window x:Class="WindowsFTPClient.Views.PromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WindowsFTPClient.Views"
        mc:Ignorable="d"
        Title="Prompt" Height="157" Width="291" WindowStartupLocation="CenterOwner">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock x:Name="PromptMessageTextBlock" TextWrapping="Wrap" />
            <TextBox x:Name="PromptTextBox" />
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button x:Name="OKButton" Click="OKButton_Click" Content="OK" />
            <Button Grid.Column="1" x:Name="CancelButton" Click="CancelButton_Click" Content="Cancel" />
        </Grid>
    </Grid>
    
    
</Window>
